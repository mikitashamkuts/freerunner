import {StyleSheet} from 'react-native';

import {backgroundColorList} from '../../design';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 100,
    backgroundColor: backgroundColorList.Avatar,
    justifyContent: 'center',
    alignItems: 'center',
    overflow: 'hidden',
  },
});
