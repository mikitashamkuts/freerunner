import {StyleSheet} from 'react-native';
import {ButtonColorList} from '../../design';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: 50,
    backgroundColor: ButtonColorList.Action,
    borderRadius: 14,
  },
});
