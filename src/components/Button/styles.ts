import {StyleSheet} from 'react-native';
import {ButtonColorList} from '../../design';

export const styles = StyleSheet.create({
  container: {
    height: 56,
    width: '100%',
    backgroundColor: ButtonColorList.Default,
    borderRadius: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
